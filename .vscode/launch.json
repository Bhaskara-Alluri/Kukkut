{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "API: Run FastAPI (uvicorn)",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "API.main:app",
        "--reload"
      ],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
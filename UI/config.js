// UI/config.js
// Global configuration for the frontend. Set the FastAPI backend base URL.
// During local development, this points to uvicorn on port 8000.
// In production, override this with the deployed API URL.
window.API_BASE_URL = "http://127.0.0.1:8000";